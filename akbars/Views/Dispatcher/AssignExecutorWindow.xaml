<Window x:Class="akbars.Views.Dispatcher.AssignExecutorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:akbars.Views.Dispatcher"
        mc:Ignorable="d"
  WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="30" SnapsToDevicePixels="True" UseLayoutRounding="True"
      Background="{StaticResource GreenBackgroundBrush}">

            
                <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="👤 Назначить исполнителя" 
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Vertical">
            <TextBlock Text="Выберите исполнителя:" FontSize="14" Margin="0,0,0,10"/>
            <ComboBox x:Name="ExecutorCombo" Height="35" FontSize="14"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" 
                    VerticalAlignment="Bottom" Margin="0,20,0,0">
            <Button Content="✅ Назначить" Width="120" Height="40" 
                    Margin="0,10,15,0" Click="Assign_Click"/>
            <Button Content="❌ Отмена" Width="100" Height="40" 
                    Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
