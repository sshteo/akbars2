<Window x:Class="akbars.Views.Worker.TicketsWorker"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Мои заявки"
        Width="1280"
        Height="820"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,16">
            <TextBlock Text="Мои заявки" Style="{StaticResource TitleText}" Margin="0,0,0,8"/>
            <TextBlock Text="Все созданные вами заявки с быстрым поиском, фильтрами и доступом к полной карточке." Style="{StaticResource SubtitleText}"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Margin="0,0,12,0">
                    <TextBlock Text="Статус" Style="{StaticResource FieldLabelStyle}"/>
                    <ComboBox x:Name="StatusFilter"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="12,0">
                    <TextBlock Text="Приоритет" Style="{StaticResource FieldLabelStyle}"/>
                    <ComboBox x:Name="PriorityFilter"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="12,0">
                    <TextBlock Text="Поиск по описанию" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox x:Name="SearchBox"/>
                </StackPanel>

                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="12,0,0,0">
                    <Button Style="{StaticResource SecondaryButtonStyle}" Content="Применить" Width="120" Click="ApplyFilters_Click"/>
                    <Button Content="Обновить" Width="120" Click="Refresh_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <DataGrid x:Name="TicketsGrid" IsReadOnly="True" SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="70"/>
                    <DataGridTextColumn Header="Создана" Binding="{Binding CreatedAt, StringFormat=dd.MM.yyyy HH:mm}" Width="150"/>
                    <DataGridTextColumn Header="Обновлена" Binding="{Binding UpdatedAt, StringFormat=dd.MM.yyyy HH:mm}" Width="150"/>
                    <DataGridTextColumn Header="Краткое описание" Binding="{Binding ShortDescription}" Width="2*"/>
                    <DataGridTextColumn Header="Тип" Binding="{Binding TypeName}" Width="150"/>
                    <DataGridTextColumn Header="Приоритет" Binding="{Binding PriorityName}" Width="140"/>
                    <DataGridTextColumn Header="Статус" Binding="{Binding StatusName}" Width="140"/>
                    <DataGridTemplateColumn Header="Карточка" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Открыть" Tag="{Binding Id}" Width="90" Click="OpenDetails_Click"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Style="{StaticResource SecondaryButtonStyle}" Content="Закрыть" Width="120" HorizontalAlignment="Left" Click="Back_Click"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Style="{StaticResource DangerButtonStyle}" Content="Удалить неназначенную" Width="190" Click="DeleteTicket_Click"/>
                <Button Content="Создать заявку" Width="170" Click="CreateTicket_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
